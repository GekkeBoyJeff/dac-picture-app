# Wachtwoord voor het remote admin panel (/remote).
# Dit wachtwoord is zichtbaar in de JS bundle — prima voor een photobooth.
# Zet dit ook in GitHub Actions Secrets als NEXT_PUBLIC_REMOTE_PASSWORD.
NEXT_PUBLIC_REMOTE_PASSWORD=changeme
